<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>

	<li><%= link_to "delete", tag_path(@tag), method: :delete, data: {confirm: "Delete the tag?"} %></li>
	<% @tag.articles.each do |article| %>
	<li><%= link_to article.title, article_path(article) %></li>
	
	<% end %>

	</ul>